# Shared (copy to .env in frontend/backend)
REACT_APP_API_URL=http://localhost:5000/api  # Frontend only; update for prod
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/uberrr  # Backend only
JWT_SECRET=your_super_secret_jwt_key_here  # Backend only
PORT=5000  # Backend only
GOOGLE_MAPS_API_KEY=your_google_maps_key  # If using maps
